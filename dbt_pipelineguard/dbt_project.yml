name: pipelineguard
version: 1.0.0
config-version: 2

profile: pipelineguard

model-paths:
  - models

models:
  pipelineguard:
    staging:
      +materialized: view
    marts:
      +materialized: table
